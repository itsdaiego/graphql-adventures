# Contributing

### Choose a category from `https://developer.github.com/v3` eg: Users and apply these changes:

- Create the schema on API and fetch the desired data
- Create a simple react App using Relay that display the fetched data
- Open a PR


simple as that :)
